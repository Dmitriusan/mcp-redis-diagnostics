{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.dmitriusan/mcp-redis-diagnostics",
  "description": "MCP server for Redis diagnostics — analyze memory usage, slowlog, client connections, and keyspace health with AI-powered recommendations",
  "repository": {
    "url": "https://github.com/Dmitriusan/mcp-redis-diagnostics",
    "source": "github"
  },
  "packages": [
    {
      "registryType": "npm",
      "identifier": "mcp-redis-diagnostics",
      "version": "0.1.3",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "name": "REDIS_URL",
          "description": "Redis connection string (e.g. redis://localhost:6379 or redis://:password@host:6379)",
          "required": false,
          "default": "redis://localhost:6379"
        }
      ]
    }
  ]
}
